class Solution:
    def findMin(self, nums: List[int]) -> int:
        hello = sorted(nums)
        return hello[0]



        # left = 0
        # right = len(nums) - 1
        # ans = 0
        # for i in range(len(nums)) :
        #     mid = (left + right )//2

        #     if 

